# include<iostream> 
using namespace std;
int main(){ 
	// Un arreglo u array es un conjunto de datos almacenados separados por una coma(,) en una misma variables, ademas en c++ debemos poner el tamaño de ese arreglo  lo que es el numero de datos que contendra ese arreglo
	 int numeros[6]={1,2,3,4,5,6};
	 char letras[5]={'a','b','c','d','e'};
	 
	 // Para acceder a los datos del arreglo lo hacemos medainte el indice donde se encuentra eld ato al que euremos acceder del arreglo
	 cout<<numeros[4]<<endl;
	 cout<<letras[2];
	
	
	
	return 0; 
}
