# include <iostream> 
using namespace std;
int main(){ 
/* 
El ciclo for cumple con la misma funcionalidad que el ciclo while y do while con la diferencia que este es el ciclo mas optimo de usar
dentro del parentesis recibe tres parametros el primero es la variable de inicio, despues viene la condicion y bpor ultimo viene la razon de 
incremento o decremento
*/

for(int item=0;item<=10;item++){
	cout<<"Hola Mundo desde C++"<<endl;
};



return 0;
}
